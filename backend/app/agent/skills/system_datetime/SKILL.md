# system_datetime

当用户询问**当前日期、时刻、星期**或任何依赖「现在」的答案时，应调用工具 `get_system_datetime` 获取服务器时间，**不要**臆造或沿用训练数据中的日期。

适用场景示例：「现在几点」「今天星期几」「当前 UTC 时间」「截止日期距今还有多久」等。

工具返回 ISO-8601 字符串与 Unix 时间戳；需要本地时区时可传 `timezone: "local"`，否则默认 UTC。
